# Ensure shell scripts always use LF, even when checked out on Windows.
# Without this, git's default `core.autocrlf=true` on Windows rewrites
# the script with CRLF and bash dies on `\r` tokens at every line end.
*.sh text eol=lf
